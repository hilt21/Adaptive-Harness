[project]
name = "python-fixture"
version = "0.1.0"
dependencies = ["fastapi>=0.100", "asyncpg>=0.29"]

[project.optional-dependencies]
test = ["pytest>=9"]

[tool.pytest.ini_options]
testpaths = ["tests"]

